# .aspect/workflows/config.yaml

tasks:
  - ci: # Tarefa agregada chamada pelo main.yaml (task: ci)
      run_tasks: 
        - build
        - test

  - build:
      targets:
        - "//packages/hello-world:devserver"
  
  - test:
      targets:
        - "//packages/hello-world/..."